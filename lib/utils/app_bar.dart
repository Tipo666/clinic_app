import 'package:flutter/material.dart';

class ClaseAppBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }

  AppBar metodoAppBar(Text texto){
    return AppBar(title: texto,);
  }
}
